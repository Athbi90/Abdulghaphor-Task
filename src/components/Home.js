import { PageTitle, SmallTitle } from "../styles.js";
const Home = (props) => {
  return (
    <div>
      <PageTitle>Hello World</PageTitle>
      <SmallTitle>This is homepage</SmallTitle>
      <p>Nothing much, just homepage</p>
    </div>
  );
};

export default Home;
